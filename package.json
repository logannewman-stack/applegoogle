{
  "name": "applegoogle",
  "version": "0.1.0",
  "description": "A minimal, ad-free search engine. Organic ranking only — no sponsored results, ever. Subscription-funded.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.js",
    "seed": "node scripts/seed.js",
    "crawl": "node scripts/crawl.js",
    "create-user": "node scripts/create-user.js",
    "test": "node --test"
  },
  "dependencies": {},
  "devDependencies": {}
}
